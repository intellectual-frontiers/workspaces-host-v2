# Nix
result
result-*
.direnv/

# Spec Kit machine-local state
.specify/feature.json
.specify/extensions/*/local-config.yml

# OS/editor cruft
.DS_Store
*.swp

# Agent folder security: avoid committing credentials/auth tokens some
# agents may cache locally under .claude/.
.claude/settings.local.json
.claude/*.local.json

# Claude Code background-agent isolated git worktrees - session-local
# scratch state, never part of this project.
.claude/worktrees/
